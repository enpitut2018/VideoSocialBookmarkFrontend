language: node_js

node_js:
  - "stable"

cache:
  yarn: true
  directories:
    - node_modules

install:
  - yarn

before_script:
  - yarn mock &
  - "sleep 5" # Wait for the mock server to start

script:
  - yarn test
  - yarn lint
